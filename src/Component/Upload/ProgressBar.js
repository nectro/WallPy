import React from 'react'
import useStorage from '../Hooks/useStorage'
import classes from '../Upload/ProgressBar.module.css'

const ProgressBar = () => {
     return (
          <div className={classes.ProgressBar}>progress</div>
     )
}
export default ProgressBar